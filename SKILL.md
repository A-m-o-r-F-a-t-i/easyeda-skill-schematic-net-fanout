---
description: 用于嘉立创原理图设计、模糊需求主动补全、用户中途介入和同板最新有效修改的增量PCB同步；覆盖选型、库引脚封装核对、局部连线、分区美化、接口说明与网表验收。方向性选择询问用户，其余合理细节主动完成。含试验性大功率、高速及FOC审查，不承担PCB具体布局布线或通用Bridge维护。
metadata:
    version: 2.1.4
name: easyeda-schematic-net-fanout
---

# 嘉立创原理图增强

从电路需求、器件依据和真实引脚开始，交付能够审查、继续布板和复用的原理图。局部电路优先用短导线呈现拓扑，跨页、跨功能区和密集主控边界使用必要的网络端口。逐引脚扇出只是其中一种表达方式，不能替代完整电路绘制。

## 默认交付方式

主动把模糊目标补成可用电路和清楚图纸，先利用当前工程与已知偏好决定合理细节。说明影响理解或接线的推定，只有无法从资料确定且会改变功能、供电、兼容性、机械边界或成本方向的选择才询问用户，不让用户逐项指定常规细节。

任何阶段都接受用户新指令和编辑器里的修改。下一次写入/切页/ECO前重读，保留用户的有效修改，更新过期计划与受影响验证。用户要求更新PCB时，默认比较同一目标板的全部最新有效差异，包含最初没有逐项说出的修改，核查后增量应用并更新相关铜与丝印；明确的排除项继续保留。

完整协作规则见 [需求补全与ECO](references/intent-and-eco.md)，开始设计与同步任务时读取。以其默认范围和介入规则解释后续的局部保护要求；局部保护用于防止误改，不把最初任务清单冻结为以后不可扩充的范围。

## 1. 职责与按需加载

| 任务 | 读取内容 |
| --- | --- |
| 模糊需求、中途修改、更新PCB与接口交付 | [需求补全与ECO](references/intent-and-eco.md)；行为复测见[协作案例](references/collaboration-cases.md) |
| 选择器件、降低BOM成本、替换稳压器/接口/按键、增加保护 | [器件选型与替代](references/component-selection.md) |
| 调整分区、排阻、接口、字号、导线或跨页连接 | [版面与连接表达](references/schematic-layout.md) |
| 在线绘制、读回引脚、处理标签或修改既有电路 | [原理图API与ECO](references/api-and-eco.md) |
| 大功率、高速、FOC专题 | [试验性电路审查](references/experimental-design.md) |
| 需要一个完整表达范例 | [功能块表达示例](references/esp32-s3-debugger-example.md) |

通用API签名、Bridge与库检索能力由 `easyeda-api` 提供。PCB工程决策交给 `easyeda-pcb-layout-routing`。宿主工具路由、审批和通用恢复规则不写入本领域Skill。

## 2. 设计输入与可信状态

写图之前读取当前工程、页、单位、符号、器件属性、真实引脚、已有导线和网络。恢复旧任务时以实际文档为准，不用聊天中的器件数量或型号替代读回。

主动从既有工程和已给信息补全供电范围、持续/峰值负载、逻辑电平、外接设备、端口方向、调试/启动保留脚、制造和采购约束。可逆的表达细节直接决定。关键电气输入经查询仍未知时，问清会改变设计方向的最少参数，继续不依赖它的工作，不能擅自填入额定电流或保护阈值。

建立三张任务内工作表：

- **器件表**：功能、制造商完整料号、候选/确认状态、原厂资料版本、商城编号、库UUID、封装和替代影响。
- **连接表**：器件位号、引脚编号、功能、网络名、信号方向、供电域、上电默认状态和保护链路。
- **页面表**：功能块、块间端口、页面位置、阅读方向、需要突出显示的电压/电流/配置条件。

表格属于工程资料，不把某块板的私有ID、坐标或BOM固化到Skill中。

## 3. 选型与库验证

先确认电气适配，再比较封装、装配、供货和总成本。额定值取自对应后缀和封装的数据手册，商城名称仅用于定位候选。器件从候选晋升到可放置状态至少需完成：

1. 对照完整料号验证工作范围、关键限制与外围要求。
2. 核对符号引脚编号、内部连接、封装焊盘编号、极性和散热焊盘。
3. 核对典型应用中不可省略的上拉、去耦、补偿、反馈、时序或保护器件。
4. 替换时列出引脚/参数/封装/网表变化，并读取替换后的真实引脚重新连接。

选型未确认的器件保留候选标记，不因为成功放入库器件就认为电路成立。

## 4. 页面布局与可读连接

先画最小完整功能块，再按电源、控制、通信、驱动和接口依赖排页面。信号尽量从左向右，电源向上、地向下；符号引脚方向或拓扑需要改变时，以读图清晰和接线正确为准。

**局部直接连线**：反馈分压、去耦、串联电阻、ESD、晶体、按键、收发器外围用短导线展示完整连接。已用连续导线连接、且没有调试或跨区引用价值的网络不重复加标签。

**边界端口**：跨区/跨页、主控密集引脚和外部接口需要网络名时使用端口或标签。端口的连接点必须落在真实导线上，方向按输入/输出/双向功能设定，不能全部无条件设为BI。

**块内几何**：在同一网格对齐器件、无源件中心线和端口。先调整元件朝向和位置，再布正交导线，减少交叉、折返和过长支路。原理图正交拐角是正常表达，不继承PCB的禁止90°铜线规则。

**分区与文字**：矩形框围住该电路的器件、引脚外伸、属性和注释，框内保留空白。标题形成稳定层级，正文和器件属性在整页缩放下仍可读。装饰框和颜色不能遮挡电气信息。

## 5. 排阻、ESD与接口组合

从真实MCU引脚侧和外部接口两端同时规划保护链路。排阻优先组合物理上相邻且要求相容的信号，保持阵列内部配对。禁止仅为整齐重映射固定引脚、交换非等效功能或把不同阻值需求塞进同一阵列。

`芯片侧网络 → 独立串联电阻通道 → 接口侧网络` 两侧使用不同网络名，避免同名标签短接保护器件。ESD实际接入接口侧节点，其公共脚按内部结构接到规定参考点。通过网表确认每个外接信号经过指定通道，不能只检查器件是否存在。

电源针、地针、CAN差分总线、模拟输入、I²C与普通推挽UART分别核对保护需求。统一采用“排阻＋ESD”的外观不能省略各类信号的电容、泄漏、压降和时序检查。

## 6. ECO与既有图修订

修改前保存功能和连接表，界定受影响器件、引脚及网络。新增接口前查全部页和网表，避免重复引出已有功能。删除重复接口只清理其专属器件和专属线，不破坏共享供电或标签。

移动器件、换回旧按键或替换稳压器后，重新获取引脚连接点，重接局部导线，清理孤立短线与无用端口。以最新网表逐项区分本次改动、用户并行修改与意外变化。保留并合入有效人工改动，不能把受影响范围外的一切变化都判为错误。不要用大矩形范围批量删除“无位号对象”，其中可能包含共享电源标志和其他分区标签。

用户仅要求工程中版式修改时，不修改公共器件库的作者、符号或封装。明确要求保存个人库修改时，应在已授权的个人库对象中编辑，并检查项目引用是否确实更新。

## 7. 验收

**电气**：逐引脚比对连接表与网表，检查供电、地、极性、NC/保留脚、内部短接脚、保护前后网络、重复位号、未连接、跨页同名与意外短接。运行原生电气检查，列出允许的有意未连项及依据。

**视觉**：整页检查阅读顺序、分区密度和标题层级；逐块检查属性与引脚/导线/框线重叠、文字截断、连接点、长线和冗余标签。原理图截图用于版面核对，网络连接以数据读回为准。

**交付**：保存后重读受影响页、器件与网表，确认替换料号和外围参数。已请求更新PCB时，以同板全部最新有效差异生成ECO，无冲突项默认一并同步，不反复请求确认。保留PCB用户布局和仍有效的铜，修复电气变化影响的连线、重铺和接口丝印，明确暂缓的冲突组。全差异核对采用增量写入，不清空PCB重导。

## 8. 证据等级

成熟经验包括可复现的引脚读回、局部可读布线、版面分层、差异核对后的增量ECO及网表对照。原厂参数仅适用于具体型号和资料版本。高级专题保持 `EXPERIMENTAL`，资料核验、计算、仿真和样板测试分别记录，不把写入规则视作实板验证。

## 环境变量

本Skill不需要环境变量。在线操作依赖已连接的EasyEDA接口，使用前确认客户端与API实际能力。

## 同步工具的选择性限制

公开`importChanges`及当前PCB MCP的导入工具整板应用，没有逐项排除参数。存在明确排除或源端冲突时，未取得可验证的选择性ECO能力就暂缓该板整体导入；可以继续不依赖它的局部工作。额外修改均已有效且无排除时，仍按默认规则一并同步。并行窗口始终保留精确工程/页目标，不借用其他线程的活动文档。

## 同步目标与实际结果

使用PCB MCP 1.6时，在prepare之后为已核实的源端修改提供expectedAfter后置条件，并在import后重读目标。器件使用源端uniqueId；焊盘网络可使用当前真实primitiveId，或以componentUniqueId加padNumber定位当前器件的逻辑针号。可能更换封装或重建物理ID时优先逻辑目标，源身份重复、父关联缺失或针号不存在时保持未匹配，不沿用旧ID或猜测新ID。一个逻辑针号有多个物理形状时全部网络必须匹配。原生import返回true、摘要未变或器件计数相同，均不能单独证明所需改动已同步。工具报告目标未达时保留现场，检查源端、关联和导入结果，不盲目重发。未提供后置条件时保留postconditionsVerified=null并逐项人工数据核对。

本轮PCB侧临时位号差异没有被原生导入自动回填，后置条件正确识别该情况并完成恢复。这不构成源端改针、增加器件、删除器件或换封装的实测通过。
